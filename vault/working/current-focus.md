# Current Focus

Not yet defined. There is a single branch (`main`), no open work-in-progress
files, and no TODO markers anywhere in the repo to infer focus from. The
only activity so far has been setting up repo tooling (`CLAUDE.md`, the
vault itself, and the `/commit` and `/vault-audit` skills).

Update this file once active development starts.
